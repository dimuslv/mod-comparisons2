- Collision query bitmap gets filled in initially??
- Ghost comparisons
- walljump/release-jump cleanliness indicator (by changing player color)?
- Freeze when focus on input field
- arbitrary expression viewer
- show var (delta) on pattern activation, or maybe as a "frame-perfect"? Second option is more expressive ig. Syntax: ?(wall_count) ?
- runup, oninput savestates (syntax: $10, $r, ig in the same place where the savestate is? so more like $10 |d). This should work even if the runup is in the beginning animation, although might be a bit tricky to implement.
- true savestates. Saving needs to be quick (maybe savestates partly premade during standard gameplay) and they should hopefully take little space, loading can be a bit slow. Automatic savestate modes: on, only on non-standard gameplay, off. Also can change whether savestates are used? Automatic savestates are hidden in the string by default, you can press a hotkey to show them as "#". When saving a state, automatic savestates are saved if they are currently visible? Can add new automatic savestates by adding the symbol, if you remove it, I think it gets automatically added back in on next pass. Manual savestates are probably not used as helper savestates, except... if saving a state without helper savestates visible, in the saved state it itself counts as a helper savestate?
- true savestates are identified by their compressed string. Can also compress a string just if you like (could be convenient for automatic reduction)
- there are save slots and savestates. Each savestate needs to be linked to at least one save slot, otherwise gets deleted. Current gamestate is a save slot, as is every manual savestate slot.
- Maybe true savestates load to the previous frame and then advance by one...
- Use () for code-based pattern start
- Simplest way to save preferences might be to just have a file that has code that sets the preferences when launching the mod
- default settings and keybinds. Probably a key for showing/hiding keybinds in the menus. Click on [] and you can choose a key.

Optional ideas:
- Saving when damage was taken in a string and using that when instant reloading?
- For specific values, probably <x=5,6>
- Patterns can detect inputs done before it started (maybe after as well) by just saving all inputs that were done in last frames in a queue
- add pattern start on frame-perfect?? Syntax: ".> ,> .<> ,<>", "*" denotes normal pattern start (not obligatory in most cases)
- multiple branches for offset string??